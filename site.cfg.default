[compiler]
# compiler_name = icc
# linker_name = icc -shared
# extra_libs = /usr/lib

[mkl]
# root = ~/.local/lib:/usr/lib
interface32 = True
sdl = False

[threading]
omp_loops = True
threaded_mkl = False
